[package]
name = "ds-check"
version = "0.1.0"
edition = "2024"
description = "CLI tool for checking DeepSeek platform usage and balance"
license = "MIT"
repository = "https://github.com/MysticalDevil/ds-check"
keywords = ["deepseek", "cli", "balance", "monitoring"]
categories = ["command-line-utilities"]

[dependencies]
clap = { version = "4", features = ["derive", "color"] }
reqwest = { version = "0.13", features = ["json"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
dirs = "6"
chrono = "0.4"
unicode-width = "0.2"
ratatui = "0.30"
crossterm = "0.29"
sha2 = "0.10"

[profile.release]
strip = true
lto = true
